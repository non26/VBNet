Module createArrayWithSize
    Sub createArrayWithSize1()
        Dim test As Integer()
        'For Each item As Integer In test
        '    Console.WriteLine(item.ToString)
        'Next
        Console.WriteLine(test.Contains(0))
    End Sub

End Module
